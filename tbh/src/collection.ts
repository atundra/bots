// import { pipe } from 'fp-ts/lib/function';
// import { Db } from 'mongodb';
// import * as m from './mongo';

// const dbName = () => 'tbh-test';

// // export const get = (name: CollecionName) => (mc: m.ConnectedMongoClient) => pipe(mc, m.db());
